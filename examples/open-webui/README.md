# Open WebUI

Configure a custom OpenAI-compatible endpoint:

- API Base URL: `http://127.0.0.1:9000/v1`
- API Key: your upstream-compatible token

If model discovery is enabled, `GET /v1/models` should return the ids you want to expose.
